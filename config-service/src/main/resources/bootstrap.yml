spring:
  application:
    name: config-server
  profiles:
    active: git
  #    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SerikMeiirbek/ticketing-project-microservice-config.git
          default-label: master
          username: serik.meiirbek@gmail.com
          password:
      fail-fast: true

#      native:
#        search-locations: classpath:/config